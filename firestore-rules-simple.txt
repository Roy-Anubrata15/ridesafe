rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for testing - you can make this more restrictive later
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
} 